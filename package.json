{
  "name": "smart-study-tracker",
  "version": "1.0.0",
  "description": "Smart Study & Productivity Tracker - INFO2413 Project",
  "private": true,
  "scripts": {
    "dev:db": "docker compose up -d || docker-compose up -d",
    "dev:db:stop": "docker compose down || docker-compose down",
    "dev:db:reset": "(docker compose down -v && docker compose up -d) || (docker-compose down -v && docker-compose up -d)",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend/smart-study-tracker && npm run dev",
    "dev:all": "npm run dev:db && sleep 5 && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend/smart-study-tracker && npm install",
    "db:logs": "docker compose logs -f postgres || docker-compose logs -f postgres",
    "db:shell": "docker exec -it smart_study_db psql -U postgres -d smart_study_tracker",
    "setup": "npm run install:all && npm run dev:db && echo '\n✅ Setup complete! Wait 10 seconds, then run: npm run dev:all'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/locava/INFO2413.git"
  },
  "keywords": [
    "study-tracker",
    "productivity",
    "education",
    "ai-reports"
  ],
  "author": "INFO2413 Team",
  "license": "MIT"
}

