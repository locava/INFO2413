# Smart Study Tracker - Environment Variables Template
# Copy this file to .env and update values as needed

# Database Configuration (Docker defaults - no changes needed for local dev)
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_study_tracker

# Backend Server
PORT=5001

# Session Secret (change in production)
SESSION_SECRET=study_tracker_secret_key_2024_info2413

# OpenAI API Key (optional - for AI features)
# OPENAI_API_KEY=your_openai_api_key_here

