# Backend Environment Variables
# Copy this to .env and update as needed

# Server Configuration
PORT=5001
NODE_ENV=development

# Database Configuration (matches Docker defaults)
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5433
DB_NAME=smart_study_tracker

# Session Configuration
SESSION_SECRET=study_tracker_secret_key_2024_info2413

# Email Configuration (Optional - will simulate if not configured)
# For Gmail: Enable 2FA and create an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@studytracker.com

# Logging
LOG_LEVEL=info

# Scheduler (set to false to disable automated tasks)
SCHEDULER_ENABLED=true

# System Configuration
MIN_STUDENTS_FOR_AGGREGATES=5
WEEKLY_MIN_HOURS=10
DEFAULT_FOCUS_LOSS_MINUTES=60

# Optional: OpenAI API Key for AI features
# OPENAI_API_KEY=your_openai_api_key_here
